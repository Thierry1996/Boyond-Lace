# Mega-menu images

Orientation images for the header mega-menu columns.
Served at **`/media/images/menu/<file>`**.

- Landscape ~4:5 / 1:1 reads best in the menu's image panel. `.webp` preferred,
  ≤ ~1200px, compressed.
- Name each file after the nav column/group it belongs to
  (e.g. `menu-shop.webp`, `menu-glueless.webp`, `menu-wholesale.webp`,
  `menu-learn.webp`), then tell me which column each maps to — I wire it into the
  mega menu (today it renders named gradient keys; I'll point the column at your
  file).

## Wired columns
_(none yet — added here as they're wired)_
